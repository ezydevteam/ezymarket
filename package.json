{
    "private": true,
    "scripts": {
        "dev": "echo 'No build tool configured. Use Vite if needed.'",
        "production": "echo 'No build tool configured. Use Vite if needed.'",
        "vite-dev": "vite",
        "vite-build": "vite build"
    },
    "devDependencies": {
        "axios": "^0.21",
        "bootstrap": "^4.6.0",
        "jquery": "^3.6",
        "lodash": "^4.17.19",
        "popper.js": "^1.16.1",
        "postcss": "^8.1.14"
    },
    "dependencies": {
        "@ckeditor/ckeditor5-dev-translations": "^39.6.1"
    },
    "comment": {
        "build-system": "This project does NOT use a build tool by default.",
        "assets": "All assets are pre-compiled in public/vendor/ and public/themes/",
        "vite-optional": "To add Vite, run: npm install --save-dev vite laravel-vite-plugin",
        "node-required": "Node.js is not currently installed. Install from https://nodejs.org/ to use npm commands."
    }
}

