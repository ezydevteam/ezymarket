<span role="button" class="fw-bold"
    data-action="{{ route('admin.products.categories.sub-categories.edit.modal', $subCategory->id) }}"
    data-bs-toggle="modal" data-bs-target="#editSubCategoryModal">{{ e($subCategory->name) }}
</span>
