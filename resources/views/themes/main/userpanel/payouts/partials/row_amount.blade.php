<div class="d-flex flex-column lh-sm">
    <span class="text-dark fw-bold mb-1">{{ $payout->amount_info->amount }}</span>
    <span class="xsmall text-muted">{{ $payout->amount_info->label }}</span>
</div>
